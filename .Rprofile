# Auto-activate renv when you enter the project
source("renv/activate.R")

# Use a stable CRAN mirror & quiet down numeric scientific notation
options(
  repos  = c(CRAN = "https://cloud.r-project.org"),
  scipen = 999
)

# Optional: use the shared cache for speed (default TRUE)
renv::settings$use.cache(TRUE)
